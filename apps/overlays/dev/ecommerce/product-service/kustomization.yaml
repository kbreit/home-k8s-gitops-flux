apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../base/ecommerce/product-service
  - ./networkpolicy.yaml
  - ./ciliumnetworkpolicy.yaml
  - ./database.yaml
patches:
  - patch: |-
      - op: replace
        path: /spec/ref/semver
        value: "0.4.1"
    target:
      kind: OCIRepository
      name: product-service
  - patch: |-
      - op: replace
        path: /spec/values
        value:
          ingress:
            host: product.home.kevinbreit.net
          image:
            tag: "0.2.4"
          imagePullSecrets:
            - name: ghcr-token
          envFrom:
            - secretRef:
                name: user-service-secret
            - configMapRef:
                name: product-service-config
          migrate:
            enabled: true
            command:
              - ./migrate
          database:
            host: ecommerce-dev-rw.cnpg-system
            database: product-service
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
    target:
      kind: HelmRelease
      name: product-service
