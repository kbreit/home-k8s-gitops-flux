apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: frontend-service
  namespace: flux-system
spec:
  interval: 10m
  url: oci://ghcr.io/kbreit/charts/frontend-service
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  secretRef:
    name: ghcr-token
  ref:
    semver: "1.0.2"
